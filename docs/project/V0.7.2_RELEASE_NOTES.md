# Church Cap v0.7.2 GitHub Submission And Release Notes

The public repository and latest public release are currently v0.7.0. The v0.7.1 operator-maintenance work was prepared locally but was not published, so the v0.7.2 pull request and release notes describe the complete public v0.7.0 → v0.7.2 change set.

## Pull Request Title

Fix v0.7.0 web updates and improve operator recovery (v0.7.2)

## Pull Request Description

### Summary

- Restore direct web updates from the published v0.7.0 release by retaining the literal version field read by its updater while keeping the running version code-owned.
- Track the detached updater process and expose a safe, local, operator-authenticated status endpoint.
- Replace the indefinite updating overlay with themed **Update stopped** and **Restart needed** states, including **Download diagnostics** and **Return to Updates** actions.
- Prevent concurrent web updates, pin a web-launched update to the exact selected GitHub tag, and close the parent update-log descriptor after launch.
- Add the prepared operator-usability improvements: a visible everyday language-settings save action, version-aware Recommended-mode wording, clearer Service Leader guidance, and clearer Diagnostics and Updates navigation icons.
- Remove the stale duplicate environment template while retaining backward-compatible setup-script fallbacks.
- Update application metadata, asset cache keys, tests, setup examples, blueprint, implementation notes, release notes, third-party notices, and the rendered action plan to v0.7.2.
- Preserve the v0.7.0 caption cue engine, translation scheduler, viewer behaviour, privacy controls, storage controls, and service-metrics schema.

### Testing

- [x] All 190 automated tests pass.
- [x] Python compilation, shipped JavaScript, extracted inline JavaScript, and every shipped shell script pass syntax checks.
- [x] Public and authenticated operator pages render successfully; the loopback-only update-status route returns the v0.7.2 state and relative update-log label.
- [x] The published v0.7.0 source archive accepts v0.7.2 through its exact legacy version parser.
- [x] A temporary-folder v0.7.0 → v0.7.2 updater rehearsal validates the archive, compiles the staged release, replaces application files, and preserves `.env` and the church glossary.
- [x] Local Markdown links, JSON, GitHub Actions YAML, version references, file inventory, ignored-runtime-file scan, secret-pattern scan, and whitespace checks pass.
- [x] The default `requirements.txt` audit reports no known vulnerabilities.
- [x] The v0.7.2 action-plan DOCX renders cleanly to 20 pages.
- [ ] Complete one real Linux appliance web update after v0.7.2 is published as GitHub's latest normal release; v0.7.0's hard-coded latest-release endpoint prevents this final network/restart test before publication.

### Screenshots

No new asset is required. The update failure/restart panels reuse the existing Church Cap overlay, glass-card, button, colour, and spacing system. Add an appliance screenshot to the pull request after the published update test if useful.

## Release Summary

Church Cap v0.7.2 restores direct updates from v0.7.0, makes updater failures visible and recoverable, and includes focused operator-interface clarity improvements without changing live caption or translation behaviour.

## Release Description

# Church Cap v0.7.2

Church Cap v0.7.2 is a maintenance release focused on reliable updates and clearer everyday operator controls. It is designed as the direct upgrade from the current public v0.7.0 release.

## Updater reliability

- Restores the direct **Operator → Updates** path from v0.7.0.
- Preserves the legacy release field required by the published v0.7.0 updater while keeping the running application version code-owned.
- Detects updater failure during the normal reconnect cycle instead of leaving an indefinite spinner.
- Offers **Download diagnostics** and **Return to Updates** when an update stops.
- Shows a distinct restart-needed state if file replacement finishes but Church Cap does not restart automatically.
- Downloads the exact release selected by Church Cap and rejects a second update while one is already active.
- Keeps update status privacy-safe: no absolute paths or raw log contents are returned by the status endpoint.

## Operator improvements included

- Adds a prominent **Save language settings** button beside the everyday translation controls while retaining the Advanced save button.
- Makes Recommended translation-mode guidance use the running Church Cap version instead of obsolete version text.
- Explains the restricted Service Leader role for new operators.
- Uses clearer Diagnostics and Updates menu icons in the existing Church Cap theme.
- Removes the stale duplicate environment template while keeping older setup checkouts supported.

## Behaviour retained

Caption timing, word-timestamp cue handling, correction-in-place, Responsive Context translation, separate Simplified and Hong Kong Traditional Chinese choices, bounded translation queues, viewer presentation, diagnostics schema, privacy boundaries, and storage controls remain unchanged from v0.7.0.

## Upgrade note

Once v0.7.2 is published as the latest normal GitHub release, users on v0.7.0 can update directly through **Operator → Updates**. The literal `app_version: str = "0.7.2"` field is intentional and must remain aligned with `APP_VERSION` until v0.7.0 upgrade support is formally retired.

If an update stops, use **Download diagnostics** on the update panel. The confirmed diagnostics export includes a redacted tail of `logs/update.log`; it does not include captions, translations, transcripts, audio, passwords, or `.env` contents.

## Validation and dependency note

All 190 automated tests pass, alongside syntax, route/render, documentation, archive, published-v0.7.0 compatibility, temporary replacement, and action-plan render checks. The default dependency audit reports no known vulnerabilities.

Optional Argos translation still resolves SentencePiece 0.2.0 (`PYSEC-2026-1909`) and Stanza 1.1.1 (`PYSEC-2026-3075`) because Argos 1.9.6 pins them. Church Cap disables the unused Stanza model pipeline; install only trusted model packages and see `docs/legal/THIRD_PARTY_NOTICES.md` for the full boundary.

Church Cap remains local-first and open-source. Operators should test their intended audio route, model, languages, and hardware before a live service. AI-generated captions and translations can contain mistakes and should not be treated as a verbatim or authoritative record.
